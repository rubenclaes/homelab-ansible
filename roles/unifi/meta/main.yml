---
galaxy_info:
  role_name: unifi
  author: rubenclaes
  description: >-
    Creates client entries and address reservations on the UniFi gateway from
    the device list, including for devices that have never connected.
  license: MIT
  min_ansible_version: "2.17"
  platforms:
    - name: Debian
      versions:
        - bookworm
        - trixie
  galaxy_tags:
    - networking

# These roles are composed by playbooks, not by role dependencies,
# so the run order stays visible in playbooks/site.yml.
dependencies: []
