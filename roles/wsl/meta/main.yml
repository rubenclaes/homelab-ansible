---
galaxy_info:
  role_name: wsl
  author: rubenclaes
  description: >-
    Ubuntu under WSL as a workstation: apt, snaps, Homebrew, Go, zsh tooling.
  license: MIT
  min_ansible_version: "2.17"
  platforms:
    - name: Ubuntu
      versions:
        - noble
  galaxy_tags:
    - development
    - system

# These roles are composed by playbooks, not by role dependencies,
# so the run order stays visible in playbooks/site.yml.
dependencies: []
