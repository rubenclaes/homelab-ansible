---
galaxy_info:
  role_name: macos
  author: rubenclaes
  description: >-
    Homebrew formulae, casks and macOS defaults.
  license: MIT
  min_ansible_version: "2.17"
  platforms:
    - name: MacOSX
      versions:
        - all
  galaxy_tags:
    - system
    - packaging

# These roles are composed by playbooks, not by role dependencies,
# so the run order stays visible in playbooks/site.yml.
dependencies: []
