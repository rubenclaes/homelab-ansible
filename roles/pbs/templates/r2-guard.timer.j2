# {{ ansible_managed }}
[Unit]
Description=r2-guard en de sync naar R2, na de nachtelijke back-ups

[Timer]
OnCalendar=*-*-* {{ pbs_offsite.guard_schedule }}:00
Persistent=true

[Install]
WantedBy=timers.target
