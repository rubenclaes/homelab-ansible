# Control-node toolchain. Pinned so a workstation cannot drift away from CI
# without noticing: .github/workflows/lint.yml installs these same versions.
#
#   pip install -r requirements.txt   # in a venv
#
# With pipx, see README: the libraries below must be injected into the
# ansible-core venv, which `pipx install -r` does not do.
#
# Bump deliberately, together with collections/requirements.yml and the
# the lint workflow, which installs from this file.
ansible-core==2.21.4
ansible-lint==26.8.0

# Needed by community.proxmox for the API modules and the dynamic inventory.
proxmoxer>=2.3
requests>=2.31
netaddr>=1.3
