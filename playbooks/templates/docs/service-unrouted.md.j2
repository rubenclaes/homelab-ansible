{% set s = item -%}
{% set h = hostvars[s.host] -%}
---
title: {{ s.name }}
---

# {{ s.name }}

{{ s.desc | default('Niet gedocumenteerd.') }}

| Eigenschap | Waarde |
| --- | --- |
| URL | — geen webroute |
| Adres | `{{ h.ansible_host | default('?') }}` |
| Host | [{{ s.host }}](/homelab/machines/#{{ s.host }}) |
| Poorten | {% for p in s.ports %}`{{ p }}`{{ ', ' if not loop.last }}{% endfor %} |
| Stack | [{{ s.stack }}](/homelab/machines/#{{ s.host }}) |

{% if s.how is defined %}
## Gebruiken

{{ s.how }}

{% endif %}
<small>Deze pagina wordt automatisch gemaakt. Iets fout? Pas `docker_services_without_route` aan in `inventory/host_vars/{{ s.host }}.yml`.</small>
