---
# The engine, on every host that runs one; then the stacks, on the host whose
# stacks this repo manages. docker-grafana-stack runs an engine but its stacks
# are not yet described here.
- name: Docker engine
  hosts: docker_hosts
  roles:
    - docker_engine

- name: Docker stacks
  hosts: docker
  roles:
    - docker_stacks
