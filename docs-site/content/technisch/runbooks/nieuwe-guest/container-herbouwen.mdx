---
title: Container herbouwen
---

import { Steps, Callout } from 'nextra/components'

# Een container opnieuw bouwen

De container staat al in `pve_lxcs`, maar bestaat niet meer op pve01. Je bouwt
hem uit de lijst terug.

<Callout type="warning">
Dit geeft je een **lege** container met de juiste instellingen. Heb je de
data nodig (instellingen, databases), zet dan een back-up terug met
[Herstellen](/technisch/runbooks/disaster-recovery/). Dat is bijna altijd wat
je wil.
</Callout>

<Steps>

### Bouw hem

```bash
ansible-playbook playbooks/new-guest.yml -e guest=adguard
```

Meerdere tegelijk kwijt? Eén per keer, in deze volgorde: `adguard` (DNS),
`caddy` (de voordeur), `tailscale`, `semaphore`.

### Doe de handwerkstappen uit `lxcs.yml`

Sommige containers hebben instellingen die de API-token niet kan zetten. Die
staan als commentaar bij hun blok in `inventory/group_vars/proxmox/lxcs.yml`.
Nu: `tailscale` (108) en `adguard` (107), allebei voor `/dev/net/tun`.

Voorbeeld voor 108, als root op pve01:

```bash
pct set 108 -lxc.cgroup2.devices.allow "c 10:200 rwm"
pct set 108 -lxc.mount.entry "/dev/net/tun dev/net/tun none bind,create=file"
pct reboot 108   # een mount-entry geldt pas na een herstart
```

<Callout type="warning">
Voor 107 weigert `pct set` die regels. Zet ze daar rechtstreeks onderaan
`/etc/pve/lxc/107.conf`. De exacte regels staan bij het blok in `lxcs.yml`.
</Callout>

### Zet de dienst er terug op

```bash
ansible-playbook playbooks/site.yml --limit adguard
```

</Steps>
